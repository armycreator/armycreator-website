{% extends 'SitiowebArmyCreatorBundle::layout.html.twig' %}

{% block h1 %}Breed creation{% endblock %}

{% block content %}
<form action="{{ path('admin_breed_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_breed') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
