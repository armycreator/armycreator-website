{% extends 'SitiowebArmyCreatorBundle::layout.html.twig' %}

{% block title %}BreedGroup creation{% endblock %}

{% block content %}
<form action="{{ path('breedgroup_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('breedgroup') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
