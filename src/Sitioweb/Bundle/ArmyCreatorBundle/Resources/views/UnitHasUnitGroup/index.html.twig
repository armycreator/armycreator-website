{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UnitHasUnitGroup list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Unitnumber</th>
                <th>Importedid</th>
                <th>Mainunit</th>
                <th>Canchoosenumber</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('unithasunitgroup_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.unitNumber }}</td>
                <td>{{ entity.importedId }}</td>
                <td>{{ entity.mainUnit }}</td>
                <td>{{ entity.canChooseNumber }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('unithasunitgroup_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('unithasunitgroup_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('unithasunitgroup_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
